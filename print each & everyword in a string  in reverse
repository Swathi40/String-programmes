#WAP to print each & everyword in a string  in reverse without using split method# 
S    = 'hello world bye world'
l  = []
word = ''
for ch in S:
    if ch!=' ':
        word=ch+word
    else:
        l.append(word) 
        word=''
l.append(word)
print(l)
